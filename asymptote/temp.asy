import trembling;
size(8cm);

path cle=unitcircle;

/* View the definition of path tremble(path,real,real,real,real) */
//path tcle=tremble(cle,frequency=0.25,random=1);
tremble tcle=tremble();
//draw(tcle);

